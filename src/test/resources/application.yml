spring:
  datasource:
    url: jdbc:h2:mem:bookingtest
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-secret-key-not-real-0123456789abcdef
    expiration-ms: 3600000
